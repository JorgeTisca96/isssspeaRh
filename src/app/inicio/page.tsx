
const apiUrl = process.env.API_URL;

export const metadata = {
    title: 'ISSSSPEA RH| INICIO',
    description: '',
};

export default async function inicioPage() {
    return (
        <></>
    );
}